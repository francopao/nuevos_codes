91 emisores tienen proxy asignado, pero el filtro `Riesgos` solo dejaba pasar 19. Quito ese filtro:

25 matches (vs solo 8 antes con el filtro `Riesgos`). Esto es correcto: de los 245 emisores VP, 25 tienen un proxy benchmark asignado en BU.

Ahora valido end-to-end con datos reales simulados:

10 barras, 6 marcadores benchmark (los 6 del top 10 que matchean con BU — Fossal/Jockey Plaza/Volcan/Gobierno Central no tienen proxy en BU, correcto). Nombres limpios sin asteriscos.

---

## BLOQUE ÚNICO

**Buscar** (dentro de `_cargar_emisor_proxy_map`):
```python
        emisor_proxy = {}
        for row in ws_bu.iter_rows(min_row=2, values_only=True):
            nombre = row[5] if len(row) > 5 else None  # col 5 = Nombre
            tiene = str(row[6] or "") if len(row) > 6 else ""  # col 6 = Tiene Huella?
            proxy = row[8] if len(row) > 8 else None  # col 8 = Proxy (FUND_NAME)
            if nombre and tiene == "Riesgos" and proxy:
                emisor_proxy[str(nombre).strip()] = str(proxy).strip()
```

**Reemplazar por:**
```python
        emisor_proxy = {}
        for row in ws_bu.iter_rows(min_row=2, values_only=True):
            nombre = row[5] if len(row) > 5 else None  # col 5 = Nombre
            proxy = row[8] if len(row) > 8 else None  # col 8 = Proxy (FUND_NAME)
            if nombre and proxy:
                emisor_proxy[str(nombre).strip()] = str(proxy).strip()
```

Y también buscar el print de log:
```python
        print(f"  [Emisor→Proxy] {len(result)} emisores con benchmark de {len(emisor_proxy)} con cobertura ESG")
```
**Reemplazar por:**
```python
        print(f"  [Emisor→Proxy] {len(result)} emisores con benchmark de {len(emisor_proxy)} en hoja BU")
```

Eso es todo. Solo se eliminó la condición `tiene == "Riesgos"` que restringía a 19 emisores, ahora lee los 91 que tienen proxy asignado en BU. De esos 91, ~25 matchean con los 245 emisores VP de Integra en BD1_S, y el chart muestra marcadores benchmark para los que caigan dentro del top 10 por Intensidad.




---

## BLOQUE ÚNICO

**Buscar:**
```python
        d = self.df[~self.df[COL["plus"]].isin(["Cash", "Cuestionario Integra"])]
        # Filtro principal: solo VP (cobertura ESG propia, no proxy)
        if "ISIN_Origen" in d.columns:
```

**Reemplazar por:**
```python
        d = self.df[~self.df[COL["plus"]].isin(["Cash", "Cuestionario Integra"])]
        # Solo universo BU
        if COL["estrategia"] in d.columns:
            d = d[d[COL["estrategia"]] == "BU"]
        # Solo VP (cobertura ESG propia, no proxy)
        if "ISIN_Origen" in d.columns:
```


