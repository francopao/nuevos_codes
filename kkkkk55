Voy a auditar el bug siguiendo tu prompt. Primero leo tu script y el archivo G&P adjunto.Necesito ver las secciones clave que el prompt señala. Voy a leer las líneas 162–720.La `operaciones()` que describe el prompt (con `dfg.columns = header1`, `del dfg['PBS']`, etc.) no coincide con la `operaciones()` real de la línea 608. Necesito ver la sección 342–541 donde probablemente esté esa lógica de columnas.Encontré el punto clave. La lógica que describe el prompt (`dfg.columns = header1`, `dfg.insert(1,'SBS2')`, `del dfg['PBS']`, rename a 25 columnas) **no está en `operaciones()`** sino en `construir_base()` (líneas 464–491 de tu versión). La `operaciones()` real (línea 608) solo hace el cálculo GAP. Esto ya es importante para tus instrucciones Ctrl+F.

Ahora voy a simular paso a paso la secuencia con tu G&P real para confirmar o descartar la desalineación. Primero preparo el entorno.La hoja tiene 55 columnas y el header 'SBS' está en fila 9 (0-based). Ahora replico **exactamente** la secuencia de `construir_base` (líneas 464–491) para ver qué columna del G&P termina mapeada a cada nombre.La estructura de columnas ya revela lo esencial. Las 15 columnas `GAP` y 15 `PBS` se alternan. El código selecciona ambas (30), inserta `SBS2`, borra las 15 `PBS`, y renombra las 15 `GAP` restantes a `INF1, HAF1GAP, ... INF3, ..., SIF3GAP`. Ahora completo la construcción y examino los 5 SBS fantasma directamente.Ahora entiendo la semántica. Fila 8 muestra las AFP (`IN, HA, RI, PF, SI`) y fila 7 los fondos (`F1, F2, F3`). Cada AFP×Fondo tiene un par `GAP`+`PBS`. El renombrado mapea la columna `GAP` de **Integra** a `INF1/INF2/INF3` (holding directo) y las demás a `XXFnGAP`. El mapeo de columnas es coherente. Ahora la prueba decisiva: veo los valores reales de los 5 SBS fantasma.Dato decisivo: para los 4 primeros SBS, `INF1` (columna Integra-F1-GAP) **sí es 0** — el mapeo de columnas es correcto, `INF1=0` se lee bien. Y `22201112601A` **no existe** en G&P (0 filas). Entonces la desalineación de columnas queda **descartada**: no es la causa. El peso positivo debe inyectarse en otro punto. Voy a verificar contra tus outputs reales.Muy revelador. Los phantom **no** entran por el peso `INF1` directo — entran clasificados como **SOB** (soberanos), **TD** y uno como **BU**. Colombia/México solo aparecen en SOB; Vanguard/VanEck en TD; `22201112601A` en BU. Necesito ver cómo `procesar_rama` arma esas estrategias y de dónde sale el peso. Leo esa sección.Necesito ver cómo se combinan y **normalizan** BUIN/TDIN/SOB en el BD1_S final, porque el peso de salida no es el `INF1` crudo. Leo 1010–1300.Ahora tengo el flujo completo. El `Weight` de salida es el valor crudo `INF1/INF2/INF3` que sale de `df_result` (no se re-normaliza después para F1/F2/F3). Entonces si `INF1=0`, `df_result('INF1', df_TD)` debería eliminarlo. Voy a **reproducir** ese paso exacto para VanEck y validar el mapeo de columnas contra un instrumento con Integra≠0.Confirmado al 100%: `construir_base` está **correcto**. Se seleccionan 39 columnas, quedan exactamente 15 `PBS`, `del` las borra todas → 25 columnas, e `INF1` coincide exacto con Integra-F1-GAP crudo. **La desalineación de columnas queda descartada.** Ahora reproduzco `operaciones → TD → df_result('INF1')` para ver por qué sobrevive VanEck.`operaciones()` falla en mi réplica porque hay celdas no numéricas (strings/None) en filas basura; el pipeline real corre con xlwings que entrega numéricos. Voy a coercer a numérico y aislar VanEck para ver exactamente qué `INF1` fluye por `df_result`.Ahora el patrón es claro:

- **VanEck** (SBS2=52→TD): INF1=**0**, INF2=0.047, INF3=0.078. Integra F2/F3 son legítimos; solo **F1 es phantom**.
- **Vanguard** (SBS2=52→TD): INF1=INF2=INF3=**0**. Los **tres** son phantom.
- **Colombia/México** (SBS2=08→SOB): INF*=**0**. Phantom.

En todos, `INF1=0` pero el output trae peso positivo. Voy a reproducir `df_result('INF1')` con datos numéricos reales para ver si de verdad no elimina, o si el peso viene de otra AFP.Con datos numéricos limpios, `df_result('INF1')` **sí elimina** VanEck y Vanguard. Es decir, el filtro funciona cuando `INF1` es 0.0 numérico. Esto es clave: significa que el peso phantom del output **no puede** ser el `INF1` de estas filas. Voy a reproducir `X_AFP` completo (TD y SOB) para Integra y ver si los phantom aparecen con este código, y de dónde saldría el peso.Resultado **definitivo**: con tu código actual reproducido fielmente, VanEck F1, Vanguard y Colombia/México **NO aparecen** — `df_result` los elimina correctamente. Pero mi reproducción coerció a numérico, y el pipeline real **no coerce**. Esa es la diferencia clave. Voy a probar cómo se comporta `df[fondo] <= 0` cuando la columna es de tipo `object` (como la entrega xlwings sin coerción), que es lo que ocurre en tu corrida real.**Encontré el mecanismo raíz.** La comparación `df[fondo] <= 0` se comporta distinto según el tipo de la columna:

- Si la celda es `0` numérico → `0 <= 0` = True → se elimina bien.
- Si la celda es **`None`/`NaN`** (celda vacía) → `None <= 0` = **False** → **la fila NO se elimina y sobrevive**.

Tu pipeline real **no coerce a numérico** (lee de xlwings tras recalcular el escenario TM4), así que las columnas quedan como `object`. Cuando una posición Integra viene vacía en vez de 0, el filtro falla y el instrumento se cuela. Ahora rastreo el origen de `22201112601A` (que no está en G&P).`22201112601A` está en **VP.xlsm/CalcP** (fuente de ISIN y nombres), no en G&P. Ahora la demostración crítica: reproduzco `df_result` **sin coerción** (como corre tu pipeline real, con columnas `object`) sobre el G&P real, para exhibir el leak de filas con `None`.Bajo los valores cacheados del archivo, **ninguna** fila TD tiene `INF1=None`, así que el leak no se dispara para estos 5 en estos datos. Esto confirma: con **este** G&P + tu código actual, los phantom **no se reproducen**. Déjame comparar con la copia del script que está en el proyecto, por si el output se generó con una versión distinta.
